# ShieldNet v4.1 Discord Explorer

Read-only synchronized inventory for channels, roles, webhooks, emojis and invites. The Discord Worker refreshes inventory every 10 minutes and through the `sync_explorer` queue job.
