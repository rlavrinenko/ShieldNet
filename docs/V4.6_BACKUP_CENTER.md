# ShieldNet v4.6 Backup Center

Creates versioned JSON snapshots of synchronized Discord configuration and ShieldNet settings. The first release provides export and a safe dry-run restore plan. Automatic apply is intentionally disabled until Discord ID remapping and rollback are implemented in the Worker.
