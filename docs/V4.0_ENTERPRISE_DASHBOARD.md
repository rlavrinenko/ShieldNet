# ShieldNet v4.0 Enterprise Dashboard

The root route now opens a consolidated command center backed by
`GET /api/v1/platform/dashboard/overview`.

It reports platform health, PostgreSQL and Valkey latency, runtime worker
heartbeats, Discord estate totals, moderation workload, security findings,
notifications, job health and managed server shortcuts.

No database migration is required for v4.0.
